#include<iostream>

using namespace std;

int main()
{
    int a, b, c; cin >> a >> b >> c;

    if (a >= b && a >= c){
        if (b <= c) cout << b << " " << a;
        else cout << c << " " << a;
    }
    else if (b >= a && b >= c){
        if (a <= c) cout << a << " " << b;
        else cout << c << " " << b;
    }
    else{
        if (a <= b) cout << a << " " << c;
        else cout << b << " " << c;
    }

    return 0;
}
